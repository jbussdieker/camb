#!/usr/bin/env ruby
require 'camb_worker'
require 'net/ssh'

puts "Starting"
Net::SSH.start('localhost', 'jbussdieker') do |ssh|
  ssh.exec!("cd Desktop/camb_worker; bundle exec bin/camb_runner") do |channel, stream, data|
    puts data
  end
end
